# Ghostty Configuration
# This config uses Hack Nerd Font Mono to properly display powerline symbols and rounded corners in tmux

# Font configuration
font-family = Hack Nerd Font Mono
font-size = 18

# Font rendering settings for better Nerd Font symbol display
font-thicken = true

# Optional: You can also specify font styles
# font-family-bold = Hack Nerd Font Mono Bold
# font-family-italic = Hack Nerd Font Mono Italic
# font-family-bold-italic = Hack Nerd Font Mono Bold Italic

# Theme (optional - Ghostty has good defaults)
# You can add Catppuccin or other themes here later

# Window settings (optional)
# window-padding-x = 10
# window-padding-y = 10

# Cursor settings (optional)
# cursor-style = block
# cursor-style-blink = true

# Other useful settings
clipboard-read = allow
clipboard-write = allow
mouse-hide-while-typing = true

# Ensure proper Unicode handling
# gtk-single-instance = true

# Keybinding fixes for tmux/SSH compatibility
# Fix Shift+Enter to work properly in tmux and over SSH
# This makes Ghostty send the same escape sequence as iTerm2 for Shift+Enter
keybind = shift+enter=text:\x1b[13;2u